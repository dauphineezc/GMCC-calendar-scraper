{
  "name": "gmcc-calendar-scraper",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo \"no build step\"",
    "start": "vercel dev",
    "postinstall": "npx playwright install --with-deps chromium"
  },
  "dependencies": {
    "dayjs": "^1.11.11",
    "playwright": "^1.46.0"
  }
}
